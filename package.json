{
  "name": "claude-terminals",
  "version": "1.0.0",
  "description": "複数ターミナルを並べて表示するデスクトップアプリ",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "postinstall": "CXXFLAGS=\"-I$(ls -d /Library/Developer/CommandLineTools/SDKs/MacOSX1[0-9]*.sdk 2>/dev/null | sort -V | tail -1)/usr/include/c++/v1\" electron-rebuild -f -w node-pty || electron-rebuild -f -w node-pty"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "electron": "^28.2.0",
    "node-pty": "^1.0.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.0"
  }
}
